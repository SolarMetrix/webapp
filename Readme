# SolarMetrix frontend application

To run this application, first make sure you have installed all the dependencies by running `pnpm install`. If pnpm is not installed on your machine, first install it globally by running `npm install -g pnpm`.

To run the application, run `pnpm run dev`, this will start the next.js app on port `3000`.
