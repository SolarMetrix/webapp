# SolarMetrix frontend application

- Install dependencies by running `pnpm install`. If pnpm is not installed on your machine, first install it globally by running `npm install -g pnpm`.
- Run `pnpm run dev`, which will start the frontend app on port `3000`.